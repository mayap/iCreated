<div class="icon-button" {{action "onClick"}} role="button">
  {{#if linkTo}}
    {{#link-to linkTo}}
      <img src={{icon}} class="icon-button__symbol" alt="">
    {{/link-to}}
  {{else}}
    <img src={{icon}} class="icon-button__symbol" alt="">
  {{/if}}
</div>
